const max = (a, b) => Math.ceil(Math.max(a, b));

console.log(max(5, 7)); // Outputs: 7 (ceil of 7 is 7)
console.log(max(10.2, 8.5)); // Outputs: 11 (ceil of 10.2 is 11)
console.log(max(-3, -1)); // Outputs: -1 (ceil of -1 is -1)
